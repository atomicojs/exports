export namespace TSCONFIG {
    const outDir: string;
    const emitDeclarationOnly: boolean;
}
export namespace TEXT {
    const start: string;
    const preparing: string;
    const noFiles: string;
    const waitingError: string;
    const waiting: string;
    const startEsbuild: string;
    const finishEsbuild: string;
    const startNoBuild: string;
    const finishNoBuild: string;
    const startPublish: string;
    const finishPublish: string;
    const ignorePublish: string;
}
